{
  "json_schema": {
    "type": "object",
    "title": "mount.umount",
    "description": "Attempt to unmount a device by specifying the directory it is mounted on",
    "properties": {
      "kwargs": {
        "type": "object",
        "title": "",
        "required": [
          "name"
        ],
        "properties": {
          "name": {
            "type": "string",
            "pattern": "^(?!.*?\\/\\/)(?!.*\\\\)\\/[^<>:,\"|?*\\0\\r\\n]+$"
          },
          "device": {
            "type": "string"
          },
          "user": {
            "type": "string"
          },
          "util": {
            "type": "string",
            "default": "mount"
          }
        }
      }
    }
  },
  "ui_schema": {
    "kwargs": {
      "name": {
        "ui:title": "Name",
        "ui:description": "Mount point path to unmount (e.g. /mnt/foo)"
      },
      "device": {
        "ui:title": "Device",
        "ui:description": "Optional device or image path to unmount instead of the mount point"
      },
      "user": {
        "ui:title": "User",
        "ui:description": "Run the underlying umount command as this user"
      },
      "util": {
        "ui:title": "Util",
        "ui:description": "Utility used to perform the unmount (mount, guestfs, qemu_nbd)"
      }
    }
  }
}
