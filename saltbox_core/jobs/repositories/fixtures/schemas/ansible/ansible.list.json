{
  "json_schema": {
    "type": "object",
    "title": "ansible.list",
    "description": "Lists available modules.",
    "properties": {
      "kwargs": {
        "type": "object",
        "properties": {
          "pattern": {
            "type": "string",
            "title": "Pattern",
            "examples": [
              "*win*",
              "*postgresql*",
              "*mysql*",
              "*user*",
              "kubernetes.core.*",
              "community.general.*",
              "ping",
              "ansible.builtin.copy"
            ]
          }
        }
      }
    }
  },
  "ui_schema": {}
}