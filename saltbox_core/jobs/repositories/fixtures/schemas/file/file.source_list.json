{
  "json_schema": {
    "type": "object",
    "title": "file.source_list",
    "description": "Check the source list and return the source to use",
    "properties": {
      "kwargs": {
        "type": "object",
        "title": "",
        "required": [
          "source",
          "source_hash",
          "saltenv"
        ],
        "properties": {
          "source": {
            "type": "string"
          },
          "source_hash": {
            "type": "object",
            "additionalProperties": {
              "type": "string"
            }
          },
          "saltenv": {
            "type": "string",
            "pattern": "^(?:_+[a-zA-Z0-9]|[a-zA-Z])[\\w-]*(?:,(?:_+[a-zA-Z0-9]|[a-zA-Z])[\\w-]*)*$"
          }
        }
      }
    }
  },
  "ui_schema": {
    "kwargs": {
      "source": {
        "ui:title": "Source"
      },
      "source_hash": {
        "ui:title": "Source hash"
      },
      "saltenv": {
        "ui:title": "Salt environment"
      }
    }
  }
}
