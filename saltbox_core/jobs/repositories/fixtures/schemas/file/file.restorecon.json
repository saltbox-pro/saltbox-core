{
  "json_schema": {
    "type": "object",
    "title": "file.restorecon",
    "description": "Reset the SELinux context on a given path",
    "properties": {
      "kwargs": {
        "type": "object",
        "title": "",
        "required": [
          "path"
        ],
        "properties": {
          "path": {
            "type": "string",
            "pattern": "^(?!.*?\\\\\\\\)(?!.*?\\/\\/)(?!.*\\\\.*\\/)(?!.*\\/.*\\\\)(?:[a-zA-Z]:\\\\|\\/)[^<>:,\"|?*\\0\\r\\n]+$"
          },
          "recursive": {
            "type": "boolean",
            "default": false
          }
        }
      }
    }
  },
  "ui_schema": {
    "kwargs": {
      "path": {
        "ui:title": "Path",
        "ui:description": "Path to reset the SELinux context"
      },
      "recursive": {
        "ui:title": "Recursive",
        "ui:description": "When recurse is set to True, the SELinux context will be reset for all subdirectories and files",
        "ui:widget": "select"
      }
    }
  }
}
