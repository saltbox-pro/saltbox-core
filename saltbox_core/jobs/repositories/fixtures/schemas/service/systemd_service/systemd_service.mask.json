{
  "json_schema": {
    "type": "object",
    "title": "service.mask",
    "description": "Mask the specified service with systemd",
    "properties": {
      "kwargs": {
        "type": "object",
        "title": "",
        "required": [
          "name"
        ],
        "properties": {
          "name": {
            "type": "string"
          },
          "runtime": {
            "type": "boolean",
            "default": false
          },
          "root": {
            "type": "string"
          }
        }
      }
    }
  },
  "ui_schema": {
    "kwargs": {
      "name": {
        "ui:title": "Name",
        "ui:description": "Systemd Service Name"
      },
      "runtime": {
        "ui:title": "Runtime",
        "ui:description": "Set to True to mask this service only until the next reboot",
        "ui:widget": "select"
      },
      "root": {
        "ui:title": "Root",
        "ui:description": "Enable/disable/mask unit files in the specified root directory"
      }
    }
  }
}
