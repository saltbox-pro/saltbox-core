{
  "json_schema": {
    "type": "object",
    "title": "service.delete",
    "description": "Delete the named service",
    "properties": {
      "kwargs": {
        "type": "object",
        "title": "",
        "required": [
          "name"
        ],
        "properties": {
          "name": {
            "type": "string"
          },
          "timeout": {
            "type": "integer",
            "default": 90,
            "minimum": 1
          }
        }
      }
    }
  },
  "ui_schema": {
    "kwargs": {
      "name": {
        "ui:title": "Name",
        "ui:description": "The name of the service to delete"
      },
      "timeout": {
        "ui:title": "Timeout",
        "ui:description": "The time in seconds to wait for the service to be deleted before returning. This is necessary because a service must be stopped before it can be deleted. Default is 90 seconds"
      }
    }
  }
}
