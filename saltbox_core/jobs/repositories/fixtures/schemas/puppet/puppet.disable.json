{
  "json_schema": {
    "type": "object",
    "title": "puppet.disable",
    "description": "Disable the puppet agent.",
    "properties": {
      "kwargs": {
        "type": "object",
        "properties": {
          "message": {
            "type": "string",
            "title": "Message",
            "description": "Disable message to send to puppet."
          }
        }
      }
    }
  },
  "ui_schema": {}
}