{
  "json_schema": {
    "type": "object",
    "title": "grains.item",
    "required": [
      "args"
    ],
    "properties": {
      "args": {
        "type": "array",
        "minItems": 1,
        "items": {
          "type": "string",
          "title": "Grain name"
        }
      },
      "kwargs": {
        "type": "object",
        "title": "Keyword arguments",
        "additionalProperties": {
          "type": "string"
        },
        "properties": {}
      },
      "sanitize": {
        "type": "boolean",
        "default": false
      }
    }
  },
  "ui_schema": {
    "args": {
      "ui:title": "List of grains",
      "ui:description": "Return one or more grains",
      "items": {
        "ui:title": "grain name"
      }
    },
    "sanitize": {
      "ui:title": "Sanitize grains",
      "ui:description": "Hide sensetive data",
      "ui:widget": "select"
    }
  }
}
