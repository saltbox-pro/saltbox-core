{
  "json_schema": {
    "type": "object",
    "title": "network.routes",
    "description": "Return currently configured routes from routing table",
    "properties": {
      "kwargs": {
        "type": "object",
        "title": "",
        "properties": {
          "family": {
            "type": "string",
            "default": "inet",
            "oneOf": [
              {
                "const": "inet",
                "title": "IPv4 routes"
              },
              {
                "const": "inet6",
                "title": "IPv6 routes"
              }
            ]
          }
        }
      }
    }
  },
  "ui_schema": {
    "kwargs": {
      "family": {
        "ui:title": "Family",
        "ui:description": "Optional address family filter, e.g. inet for IPv4 or inet6 for IPv6"
      }
    }
  }
}
