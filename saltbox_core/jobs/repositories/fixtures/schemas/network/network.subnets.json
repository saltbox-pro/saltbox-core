{
  "json_schema": {
    "type": "object",
    "title": "network.subnets",
    "description": "Returns a list of IPv4 subnets to which the host belongs",
    "properties": {
      "kwargs": {
        "type": "object",
        "title": "",
        "properties": {
          "interfaces": {
            "type": "string"
          }
        }
      }
    }
  },
  "ui_schema": {
    "kwargs": {
      "interfaces": {
        "ui:title": "Interfaces",
        "ui:description": "Optional comma-separated interface names (e.g: eth1,eth2)"
      }
    }
  }
}
