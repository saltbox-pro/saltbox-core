{
  "json_schema": {
    "type": "object",
    "title": "pillar.file_exists",
    "description": "Use the given path and search relative to the pillar environments to see if a file exists at that path",
    "properties": {
      "kwargs": {
        "type": "object",
        "title": "",
        "required": [
          "path"
        ],
        "properties": {
          "path": {
            "type": "string"
          }
        }
      }
    }
  },
  "ui_schema": {
    "kwargs": {
      "path": {
        "ui:title": "Path",
        "ui:description": "The path to the file in question. Will be treated as a relative path"
      }
    }
  }
}
