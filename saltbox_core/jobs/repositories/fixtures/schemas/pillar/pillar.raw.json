{
  "json_schema": {
    "type": "object",
    "title": "pillar.raw",
    "description": "Return the raw pillar data that is currently loaded into the minion",
    "properties": {
      "kwargs": {
        "type": "object",
        "title": "",
        "properties": {
          "key": {
            "type": "string"
          }
        }
      }
    }
  },
  "ui_schema": {
    "kwargs": {
      "key": {
        "ui:title": "Key",
        "ui:description": "Optional name of a pillar key to return a specific subtree of the raw pillar data"
      }
    }
  }
}
