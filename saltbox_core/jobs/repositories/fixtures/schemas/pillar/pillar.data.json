{
  "json_schema": {
    "type": "object",
    "title": "pillar.data",
    "properties": {
      "args": {
        "type": "array",
        "items": {
          "type": "string",
          "title": "Pillar key"
        }
      },
      "kwargs": {
        "type": "object",
        "title": "",
        "properties": {
          "pillar": {
            "type": "object",
            "additionalProperties": true
          }
        }
      }
    }
  },
  "ui_schema": {
    "args": {
      "ui:title": "List of pillar keys",
      "ui:description": "Calls the master for a fresh pillar, generates the pillar data on the fly",
      "items": {
        "ui:title": "pillar key"
      }
    },
    "kwargs": {
      "pillar": {
        "ui:title": "Pillar",
        "ui:description": "If specified, allows for a dictionary of pillar data to be made available to pillar and ext_pillar rendering. These pillar variables will also override any variables of the same name in pillar or ext_pillar"
      }
    }
  }
}
