{
  "json_schema": {
    "type": "object",
    "title": "pillar.keys",
    "description": "Attempt to retrieve a list of keys from the named value from the pillar",
    "properties": {
      "kwargs": {
        "type": "object",
        "title": "",
        "required": [
          "key"
        ],
        "properties": {
          "key": {
            "type": "string",
            "pattern": "^(?:_+[a-zA-Z0-9]|[a-zA-Z])[\\w-]*$"
          },
          "delimiter": {
            "type": "string",
            "default": ":"
          }
        }
      }
    }
  },
  "ui_schema": {
    "kwargs": {
      "key": {
        "ui:title": "Key",
        "ui:description": "The key to retrieve a list of keys from pillar"
      },
      "delimiter": {
        "ui:title": "Delimiter",
        "ui:description": "Specify an alternate delimiter to use when traversing a nested dict"
      }
    }
  }
}
