{
  "json_schema": {
    "type": "object",
    "title": "pillar.ls",
    "properties": {
      "args": {
        "type": "array",
        "items": {
          "type": "string",
          "title": "Pillar key"
        }
      },
      "kwargs": {
        "type": "object",
        "title": "",
        "properties": {
          "pillar": {
            "type": "object",
            "additionalProperties": {
              "type": "string"
            },
            "propertyNames": {
              "pattern": "^[A-Za-z_][A-Za-z0-9_]*$"
            }
          }
        }
      }
    }
  },
  "ui_schema": {
    "args": {
      "ui:title": "List of pillar keys",
      "ui:description": "Calls the master for a fresh pillar, generates the pillar data on the fly, but only shows the available main keys",
      "items": {
        "ui:title": "pillar key"
      }
    },
    "kwargs": {
      "pillar": {
        "ui:title": "Pillar",
        "ui:description": "If specified, allows for a dictionary of pillar data to be made available to pillar and ext_pillar rendering. these pillar variables will also override any variables of the same name in pillar or ext_pillar"
      }
    }
  }
}
