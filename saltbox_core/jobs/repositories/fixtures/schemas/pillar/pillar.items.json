{
  "json_schema": {
    "type": "object",
    "title": "pillar.items",
    "additionalProperties": false,
    "properties": {
      "args": {
        "type": "array",
        "items": {
          "type": "string",
          "title": "Pillar key"
        }
      },
      "kwargs": {
        "type": "object",
        "title": "",
        "properties": {
          "pillar": {
            "type": "object",
            "additionalProperties": {
              "type": "string"
            },
            "propertyNames": {
              "pattern": "^[A-Za-z_][A-Za-z0-9_]*$"
            }
          }
        }
      }
    }
  },
  "ui_schema": {
    "args": {
      "ui:title": "List of pillar keys",
      "ui:description": "Calls the master for a fresh pillar and generates the pillar data on the fly. Contrast with raw function which returns the pillar data that is currently loaded into the minion",
      "items": {
        "ui:title": "pillar key"
      }
    },
    "kwargs": {
      "pillar": {
        "ui:title": "Pillar",
        "ui:description": "If specified, allows for a dictionary of pillar data to be made available to pillar and ext_pillar rendering. These pillar variables will also override any variables of the same name in pillar or ext_pillar"
      }
    }
  }
}
